# 场景名
scenario: action

# 布局
hierarchy:
  root: actionForm

# 组件
components:
  actionForm:
    type: "ActionForm"
    props: "[后端动态注入]"
    data: {}
    operations:
      change:
        version:
          reload: true
    state:
      version: "[前端选择列表选择]"
