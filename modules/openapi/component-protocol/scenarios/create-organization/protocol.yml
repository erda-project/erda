version: 1.0
scenario: create-organization

hierachy:
  root: page
  structure:
    page:
      - createOrgForm

components:
  page:
    type: Container
  createOrgForm:
    type: Form

rendering:
  page:
    - name: createOrgForm