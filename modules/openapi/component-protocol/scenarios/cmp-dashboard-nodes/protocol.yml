scenario: cmp-Dashboard

hierarchy:
  root: page
  structure:
    page:
      - clusterFilter
      - tab
      - addLabelModal
    tab:
      children:
        - cpuTable
        - memTable
        - podTable
      tabBarExtraContent:
        - freezeButton
        - unfreezeButton
rendering:
