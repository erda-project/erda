- name: "域名访问性能告警"
  trigger: "alert"
  target: "dingding,ticket,email,mbox,webhook"
  formats:
    plt: "time:s"
  title: |
    【({{host}}) 域名访问性能告警】
  template: |
    【域名访问性能告警】

    事件: ({{host}}) 域名慢响应

    时间: {{timestamp}}

    域名: {{host}}

    平均响应时间: {{plt_avg}} ms

    [查看详情]({{display_url}})

    [查看记录]({{record_url}})