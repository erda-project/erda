scenario: transaction-http-detail

hierarchy:
  root: page
  structure:
    page:
    #- card
    - grid
    grid:
    - rps
    - avgDuration
    - slowCount
    - errorCount
components:
  page:
    type: Container
  grid:
    type: Grid
  rps:
    type: LineGraph
    options:
      asyncAtInit: true
  avgDuration:
    type: LineGraph
    options:
      asyncAtInit: true
  slowCount:
    type: LineGraph
    options:
      asyncAtInit: true
  errorCount:
    type: LineGraph
    options:
      asyncAtInit: true
