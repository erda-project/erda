http-server:
  addr: ":${HTTP_SERVER_PORT:14975}"
health:

grpc-server:
  addr: ":${GRPC_SERVER_PORT:14973}"

service-register:

erda.core.monitor.diagnotor:
  gather_interval: "${PROCSTAT_GATHER_INTERVAL:5s}"
  keepalive: "${AGENT_KEEPALIVE:15m}"
  check_keepalive_interval: "${AGENT_CHECK_KEEPALIVE_INTERVAL:1m}"
