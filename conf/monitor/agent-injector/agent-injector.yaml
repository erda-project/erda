http-server:
  addr: ":8080"
health:

http-server@admin:
    addr: ":7080"
pprof:

erda.core.monitor.agent-injector:
  webhook_addr: ":8443"
  use_insecure: false # true for test
  tls_cert_file: "/etc/server/certs/monitor-agent-injector.pem"
  tls_key_file: "/etc/server/certs/monitor-agent-injector-key.pem"
