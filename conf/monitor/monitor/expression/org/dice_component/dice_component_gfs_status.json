{
  "name": "平台组件网盘不可用告警",
  "alert_scope": "org",
  "attributes": {
    "level": "Fatal",
    "recover": "true",
    "alert_group": "{{cluster_name}}",
    "tickets_metric_key": "{{cluster_name}}"
  },
  "template": {
    "metric": "glusterfs",
    "window": 5,
    "filters": [
      {
        "tag": "org_name",
        "operator": "eq",
        "value": "$org_name"
      }
    ],
    "group": [
      "cluster_name",
      "host_ip"
    ],
    "functions": [
      {
        "aggregator": "values",
        "field": "status",
        "operator": "all",
        "value": "Disconnected"
      }
    ],
    "select": {
      "cluster_name": "#cluster_name",
      "host_ip": "#host_ip",
      "node_uuid": "#node_uuid",
      "component_name": "#component_name"
    },
    "outputs": [
      "alert"
    ]
  }
}
