{
  "name": "机器磁盘IO",
  "alert_scope": "org",
  "attributes": {
    "level": "Critical",
    "recover": "true",
    "display_url_id": "machine_disk_io",
    "alert_group": "{{cluster_name}}-{{host_ip}}",
    "tickets_metric_key": "{{host_ip}}"
  },
  "template": {
    "metric": "disk",
    "window": 5,
    "filters": [
      {
        "tag": "org_name",
        "operator": "eq",
        "value": "$org_name"
      },
      {
        "tag": "path",
        "operator": "eq",
        "value": null
      }
    ],
    "group": [
      "host_ip"
    ],
    "functions": [
      {
        "aggregator": "p75",
        "field": "pct_util",
        "operator": "gte",
        "value": 95
      }
    ],
    "select": {
      "cluster_name": "#cluster_name",
      "host_ip": "#host_ip",
      "_meta": "#_meta",
      "_metric_scope": "#_metric_scope",
      "_metric_scope_id": "#_metric_scope_id",
      "org_name": "#org_name",
      "labels": "#labels"
    },
    "outputs": [
      "alert"
    ]
  }
}
