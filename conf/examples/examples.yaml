http-server:
    addr: ":8080"
health:

grpc-server:
    addr: ":7080"

service-register:

i18n:
    files:
        - ${CONFIG_PATH}/i18n/apis.yml

# http service only
erda.example.http:
    message: "hello"

# grpc and http service
erda.example:

# if use it, or erda.example 
# erda.example-client:
