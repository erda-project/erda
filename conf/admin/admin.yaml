admin:
  debug: ${DEBUG:false}

http-server:
  addr: ":9096"
  allow_cors: true


grpc-server:
  addr: ":8096"

health:
service-register:

# provider services
erda.admin.dingtalk.DingTalkTestService:
erda.admin.marketplace.Marketplace:

# gRPC clients
grpc-client@erda.core.dicehub.extension:
  addr: "{DICEHUB_GRPC_ADD:dicehub:7080}"
erda.core.dicehub.extension-client: {}

# component-protocol framework
component-protocol:

i18n@cp:
  common:
    - conf/admin/i18n/cp/common.yaml
  files:
    - conf/admin/i18n/cp/scenarios/
i18n:
  common:
    - conf/admin/i18n/cp/common.yaml # TODO : distinguish between admin global common.yaml and cp/common.yaml

gorm.v2:
  host: "${MYSQL_HOST}"
  port: "${MYSQL_PORT}"
  username: "${MYSQL_USERNAME}"
  password: "${MYSQL_PASSWORD}"
  database: "${MYSQL_DATABASE}"