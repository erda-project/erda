# MCP Proxy Integration Tests Configuration
# Copy this file to .env and modify the values as needed

# Basic configuration
HOST=http://localhost:8081
TOKEN=
TIMEOUT=120

# Test MCP Server configuration for proxy tests
TEST_MCP_NAME=test-mcp-server
TEST_MCP_TAG=v1.0.0 